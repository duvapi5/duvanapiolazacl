---
const steps = [
  {
    number: "01",
    title: "Escucho antes de proponer",
    description: "Antes de escribir código, entiendo el problema real. El contexto define la solución correcta.",
    iconPath: "M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z",
  },
  {
    number: "02",
    title: "Priorizamos juntos",
    description: "Definimos lo esencial y establecemos expectativas claras para cada etapa.",
    iconPath: "M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4",
  },
  {
    number: "03",
    title: "Entrego, valido, ajusto",
    description: "Trabajo en ciclos cortos con entregas parciales. Avanzamos con feedback real.",
    iconPath: "M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4",
  },
  {
    number: "04",
    title: "Acompaño el crecimiento",
    description: "No desaparezco tras la entrega. Sigo presente mientras el proyecto evoluciona.",
    iconPath: "M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z",
  },
];
---

<section id="proceso" class="pt-16 pb-24 bg-slate-950 text-white relative overflow-hidden">
  <!-- Background effects -->
  <div class="absolute inset-0">
    <div class="absolute top-1/4 -left-32 w-64 h-64 bg-emerald-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-1/4 -right-32 w-64 h-64 bg-cyan-500/10 rounded-full blur-3xl"></div>
  </div>

  <div class="max-w-5xl mx-auto px-6 relative z-10">
    <div data-animate data-animation="fade-in-up" class="opacity-0">
      <div class="text-center mb-10 sm:mb-16">
        <span class="inline-block px-3 py-1 text-xs font-medium text-emerald-400 bg-emerald-500/10 rounded-full border border-emerald-500/20 mb-4 uppercase tracking-wider">
          Metodología
        </span>
        <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold mb-4">
          Cómo <span class="text-emerald-400">Trabajo</span>
        </h2>
        <p class="text-slate-400 max-w-xl mx-auto">
          Un proceso claro, colaborativo y enfocado en resultados reales.
        </p>
      </div>
    </div>

    <!-- Timeline -->
    <div class="relative">
      <!-- Connection line - desktop -->
      <div class="hidden lg:block absolute top-24 left-0 right-0 h-px bg-gradient-to-r from-transparent via-slate-700 to-transparent"></div>
      
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6 sm:gap-8">
        {steps.map((step, index) => (
          <div data-animate data-animation="fade-in-up" class="opacity-0" style={`animation-delay: ${index * 100}ms;`}>
            <div class="relative group">
              <!-- Card -->
              <div class="relative p-6 rounded-2xl bg-slate-800/30 backdrop-blur-sm border border-slate-700/50 hover:border-emerald-500/30 transition-all duration-500 h-full">
                <!-- Glow effect on hover -->
                <div class="absolute -inset-0.5 bg-gradient-to-r from-emerald-500 to-cyan-500 rounded-2xl blur opacity-0 group-hover:opacity-10 transition-opacity duration-500"></div>
                
                <div class="relative">
                  <!-- Number badge -->
                  <div class="flex items-center justify-center w-14 h-14 mx-auto mb-6 rounded-full bg-slate-900 border border-slate-700 group-hover:border-emerald-500/50 transition-all duration-300">
                    <span class="text-emerald-400 font-bold text-lg">{step.number}</span>
                  </div>
                  
                  <!-- Icon -->
                  <div class="flex justify-center mb-4 text-slate-500 group-hover:text-emerald-400 transition-colors duration-300">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d={step.iconPath} />
                    </svg>
                  </div>
                  
                  <!-- Content -->
                  <h3 class="text-lg font-semibold text-white mb-3 text-center">
                    {step.title}
                  </h3>
                  <p class="text-slate-400 text-sm leading-relaxed text-center">
                    {step.description}
                  </p>
                </div>
              </div>
              
              <!-- Arrow connector - desktop -->
              {index < steps.length - 1 && (
                <div class="hidden lg:block absolute -right-4 top-24 transform -translate-y-1/2 text-slate-700">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M9 5l7 7-7 7" />
                  </svg>
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- CTA -->
    <div data-animate data-animation="fade-in-up" class="opacity-0" style="animation-delay: 500ms;">
      <div class="mt-16 text-center">
        <p class="text-slate-500 text-sm">
          ¿Tienes un problema que resolver?
          <a href="#contacto" class="text-emerald-400 hover:text-emerald-300 ml-1 transition-colors">
            Hablemos →
          </a>
        </p>
      </div>
    </div>
  </div>
</section>
